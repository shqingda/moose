# 测试与验证

## 0.8.1：底座自主委派

发布前完整复跑 52 项单元测试与 20 项 Electron 测试，全部通过；格式、Lint、类型检查通过。Codex／Grok 用例确认无需子代理按钮即可展示底座委派活动，原生 Plan 和插话流程回归通过。本次使用确定性测试 CLI，没有调用真实模型、消耗模型 token。安装包验证见 [0.8.1 发布记录](releases/0.8.1.md)。

## 0.8.0：原生 Plan 与插话

发布前完整复跑 52 项单元测试与 20 项 Electron 用例，全部通过；类型、Lint、格式检查和生产构建通过。新增覆盖计划编辑版本、过期／重复批准、规划后暂停队列、取消使计划失效、插话投递去重、拒绝／未知结果、启动响应先于通知的时序，以及界面刷新后的持久化。已检查计划审阅和插话界面截图。构建仍有既有前端 chunk 超过 500 kB 的提示。

真实 Codex 0.155.0、`gpt-5.6-luna`／low 在临时目录完成原生计划（无文件变化）、修改范围后批准执行、同回合插话三项验证。脚本为 `pnpm exec tsx scripts/check-native-workflows.ts`，会使用模型额度。首次插话验证发现启动响应与通知的时序差异，修复并增加回归后完整验证通过。

Grok 1.0.34 ACP 握手未提供 modes，因此此阶段不宣称已支持 Grok 原生 Plan。安装包验收见 [0.8.0 发布记录](releases/0.8.0.md)。

## 0.7.0 发布验证

发版前完整复跑 41 项单元测试与 17 项 Electron 测试，均通过。格式、Lint、类型检查和 arm64 DMG 构建通过；签名严格校验及打包应用启动检查确认版本 0.7.0、sandbox 开启、SQLite 可用。详见 [0.7.0 发布记录](releases/0.7.0.md)。

## 0.7.0 开发验证：子代理接入

2026-09-18 验证：41 项单元测试通过；15 项既有 Electron 流程通过，新增 Codex、Grok 委派流程在修正测试预置会话标题后均通过。新增流程覆盖开关、请求传递、Codex 子线程审批、结果持久化及刷新恢复；已检查两种代理的界面截图。类型、Lint、修改文件格式检查与生产构建通过，构建仍提示前端 chunk 超过 500 kB。

真实 CLI 最小测试：Codex 与 Grok 各委派一个子代理计算 `2 + 2`，等待后返回 `4`。Grok 的首次测试拒绝了委派审批，确认返回拒绝结果；第二次仅批准该计算委派，成功收到结果。另一次 Codex `gpt-5.6-luna`／medium／`summary: auto` 探测回答成功，但 reasoning 事件为 0。详见[原生能力与当前缺口](providers/native-capabilities.md)。这些是开发验证，不代表发布安装包。

## 0.6.1 验证范围

35 项单元测试和完整复跑的 15 项 Electron 测试通过；真实 pnpm Pi 路径发现、版本查询、签名和安装包启动验证通过。详情见 [0.6.1 发布记录](releases/0.6.1.md)。

## 0.6.0 验证范围

本次运行通过：格式检查、Oxlint、TypeScript、生产构建、31 项单元测试、14 项 Electron 端到端测试。初始化阶段取消的保护补充后，单元测试及 Pi Electron 流程再次通过。

Pi 0.85.1 真实 CLI RPC 握手通过；环境未配置 Pi 模型，真实模型生成尚未验收。自动化的 Pi 输出、工具、确认、取消和会话恢复使用协议 fixture，不应表述为真实模型验收。Codex / Grok 的历史真实验证见 [历史验收档案](releases/validation-history.md)，其中的账号额度状态仅代表当时情况。

## 运行方法

```sh
pnpm test                  # 单元测试
pnpm test:e2e              # 构建后使用临时数据和测试 CLI 验证 Electron
pnpm test:providers        # 系统 CLI 握手和能力探测，不发送模型任务
pnpm test:live             # Codex / Grok 真实模型流程，会消耗额度
pnpm exec tsx scripts/package-smoke.ts  # 打包后的版本、签名、沙箱、SQLite
```

fixture 只用于测试，不打入应用。真实模型脚本在临时 Git 仓库工作。测试通过不代表所有代理、会员、模型和机器均经过实机验证；没有进行耗时或吞吐基准测试，不提供推测的性能提升数字。

安装包验证结果记录在对应 [Release](releases/0.6.0.md)，历史记录不覆盖。
