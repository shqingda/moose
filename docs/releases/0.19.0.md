# Moose 0.19.0

配置与扩展统一进入设置页，并补齐更多底座的用户级配置管理。

- 按底座实际能力提供 Grok Build、Pi 和 OpenCode 的配置及扩展入口；不支持的操作不展示。
- 简化 MCP 操作，固定服务器顺序，启用／禁用仅更新当前操作的反馈，保留表单草稿。
- 配置修改复用连接并跳过无关状态探测，减少等待；排除 Codex 运行时注入、没有对应配置的 MCP 条目。
- 收紧通用设置与配置来源布局，统一图标、刷新按钮和标签间距，调整会话鹿图标。
- 恢复标准 macOS 应用菜单，增加“检查更新”，打开新版发布页下载。
- 更新底座真实控制验收记录及面试指南，补充长期运行与恢复、国内面试题优先级。

发行包为 macOS Apple Silicon DMG，使用 ad-hoc 签名，未进行 Apple 公证。检查更新不包含自动下载安装。各底座仍有能力差异，详见[能力表](../providers/native-capabilities.md)。

验证：166 项单元测试、48 项桌面／Web 回归全部通过；格式、lint、类型和构建检查通过。安装包后台启动、沙箱、SQLite、Shell、PTY 布局切换、调度及应用签名检查通过。上述自动回归不调用真实模型。
