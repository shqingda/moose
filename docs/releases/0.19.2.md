# Moose 0.19.2

将 macOS 应用图标切换为第一版拟物设计：更明显的粗边框、双层高光和浮雕质感。同步更新全部图标尺寸与 ICNS。

本次仅调整应用图标和版本信息，没有功能或数据格式变更。精修版母图仍保留在仓库中；如偏好细边框，可从 [0.19.1 发布页](https://github.com/shqingda/moose/releases/tag/v0.19.1) 下载上一版安装包。

发行包为 macOS Apple Silicon DMG，使用 ad-hoc 签名，未进行 Apple 公证。

验证：格式、Lint、类型与构建检查通过，166 项单元测试和 48 项桌面／Web 回归全部通过。安装包版本、后台启动、沙箱、SQLite、Shell、PTY 布局切换、调度和严格签名验收通过；包内 ICNS 与粗边框图标资源一致。自动回归不调用真实模型。
